<view class="recent_wrapper">
    <view wx:for="{{recents}}" class="recent_item">
        <image data-id="{{item.photograph.id}}" bind:tap="onTap" class="item-image" src="{{item.pic}}" class="pic" />
    </view>
</view>